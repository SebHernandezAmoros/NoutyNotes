/** En Android no hay pestañas: los espacios en memoria se pierden al cerrar la app. */
export const MEMORY_LOSS_NOTICE = 'Los espacios del prototipo viven en memoria y se pierden al cerrar la app.';
