# Dominio

Reglas puras e invariantes, organizadas en `src/` por workspace, boards, cards, relations, templates, assets y layouts.

Implementación prevista en fases 1–4. Sin React, Expo, DOM, filesystem ni estado global. Grilla y relaciones no dependen del renderer. Las carpetas reservadas no contienen entidades implementadas.
